{
  "name": "parallel-task-scheduler-apk",
  "version": "2.7.1",
  "private": true,
  "description": "并行任务排程器 Android APK wrapper built from a single HTML app. Includes timeline task detail popup editing.",
  "scripts": {
    "build": "echo \"Static HTML app: no web build step required.\"",
    "cap:add:android": "cap add android",
    "cap:copy": "cap copy android",
    "android:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
